# DuiRichEdit 测试用例设计

> 写在产品代码之前，供确认覆盖范围与预期行为。确认后才动手实现。
> 2026-08-14 草稿。

---

## 0. 先说清楚测试跑在哪里

balloonui **没有独立测试工程，也没用第三方测试框架**。测试源文件放在 `balloonui/Tests/`，
但编译进演示程序 `DuiGallery.exe`；程序启动时在主窗口创建阶段把所有测试跑一遍，
结果写到系统临时目录下的 `DuiGallery_tests.log`。断言宏是手写的，每个测试文件各复制一份，
这是既有惯例，照抄即可。

新增一个测试文件要在三处手工登记：自己文件里的用例表、演示程序主窗口的调用列表、
演示程序的工程清单。**没有自动发现机制，漏一处就是静默不跑。**

**一个必须知道的坑**：断言宏里那个用来定位的名字参数**不会被打印出来**，
输出里只有用例表里的名字和详情字符串。所以定位信息必须写进详情里，
否则失败时只能看到"某个用例挂了"，不知道挂在哪一条断言上。

**动手前先跑一遍记基线**：仓库里现有三条测试是失败的（分隔条命中测试、框架窗口默认值、
编辑框占位文字同步），与本控件无关，不要去追。

---

## 1. 分类原则

按"需要什么才能跑"分四类，这个分法直接决定用例能不能自动化：

| 类别 | 需要什么 | 能否自动跑 | 占比目标 |
| --- | --- | --- | --- |
| A. 纯逻辑 | 什么都不需要 | 能 | 尽量多 |
| B. 需要引擎，不需要窗口 | 排版引擎对象 | **待验证**（见下） | 主体 |
| C. 布局 | DUI 控件树 | 能 | 中等 |
| D. 需要真窗口 | 宿主窗口 | 能，但脆 | 尽量少 |
| E. 只能人工核对 | 人 | 不能 | 列清单 |

**B 类能不能自动跑，取决于一个尚未验证的前提**：排版引擎能否在完全没有窗口的情况下
创建并排版。据推测可以——无窗口模式的全部意义就在于此。**第 2 步（实现引擎骨架）
第一时间验证这一条**，验证结果决定 B 类是自动测试还是并入 E 类人工核对。
这也是本设计里唯一的不确定点。

现有控件的做法值得借鉴：它把"还没有窗口"设计成一个明确的、可测的合法状态
（设值写缓存、取值读缓存、危险操作返回失败而不是崩），因此 27 条用例里 21 条不建窗口。
**新控件应当同样把"引擎尚未创建"定义成合法可测状态**，而不是未定义行为。

---

## 2. A 类：纯逻辑用例

这些是从控件里抽出来的静态纯函数，不依赖任何对象状态。抽出来的目的就是为了能零成本测。

### 2.1 单位换算

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| A-01 | 像素转百分之毫米 | 96 每英寸点数下，96 像素 = 2540 |
| A-02 | 百分之毫米转像素 | 上一条的逆运算，往返回到原值 |
| A-03 | 高每英寸点数下的换算 | 144 每英寸点数下，144 像素 = 2540 |
| A-04 | 磅值转 twip | 9 磅在 96 每英寸点数下的换算结果 |
| A-05 | 换算的边界 | 0 返回 0；负值按原样处理不崩 |

### 2.2 自动增高的尺寸夹取

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| A-06 | 内容高度在上下限之间 | 期望高度等于内容高度 |
| A-07 | 内容高度低于下限 | 返回下限 |
| A-08 | 内容高度高于上限 | 返回上限（超出部分靠滚动条） |
| A-09 | 上限设为 0（不限） | 期望高度等于内容高度，不夹 |
| A-10 | 下限大于上限（调用方设错） | 以下限为准，不返回矛盾值，不崩 |
| A-11 | 按行数设上下限 | 行数乘以默认字体行高，等于对应的像素值 |

### 2.3 滚动条策略判定

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| A-12 | 自动档，内容装得下 | 不显示 |
| A-13 | 自动档，内容装不下 | 显示 |
| A-14 | 总是档，内容装得下 | 仍显示 |
| A-15 | 从不档，内容装不下 | 仍不显示（但内容仍可用滚轮和键盘滚动） |
| A-16 | 内容高度恰好等于视口高度 | 不显示（边界，不能差一像素就冒出来） |

### 2.4 属性位拼装

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| A-17 | 默认状态拼出的属性位 | 含富文本、多行、自动换行；不含只读、密码、竖排 |
| A-18 | 逐个开关翻转后重新拼装 | 每个开关只影响自己对应的那一位 |
| A-19 | 掩码过滤 | 只返回掩码里问到的那几位，其余为零 |
| A-20 | 拖放开关与"禁止拖放"位的反向对应 | 开关为真时该位为零，开关为假时该位置起 |

### 2.5 右键菜单模型

复用现有的菜单模型纯函数，本控件只需验证接线正确。

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| A-21 | 可编辑、有选区、剪贴板有文本 | 剪切 / 复制 / 粘贴 / 全选 全部可用 |
| A-22 | 只读状态 | 只有复制和全选可用，剪切和粘贴灰显 |
| A-23 | 无选区 | 剪切和复制灰显 |
| A-24 | 空文档 | 全选灰显 |

---

## 3. B 类：需要引擎、不需要窗口

**前提：第 2 步验证"引擎可无窗口创建"通过。** 不通过则整类转入 E 类人工核对，
并在此文档中记录该结论。

### 3.1 文本读写

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-01 | 设文本再读回 | 原样返回 |
| B-02 | 设中文文本再读回 | 原样返回，不乱码 |
| B-03 | 设多行文本再读回 | 换行符保留 |
| B-04 | 设空串 | 读回空串，文本长度为 0 |
| B-05 | 文本长度 | 与设入的字符数一致（注意换行符的计数口径，用例里写明期望值的算法） |
| B-06 | 追加文本 | 追加到末尾，不影响原有选区 |
| B-07 | 清空 | 文本长度归零 |
| B-08 | 行数统计 | 三行文本返回 3 |

### 3.2 选区

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-09 | 设选区再读回 | 起止位置一致 |
| B-10 | 全选 | 起为 0、止为文本长度 |
| B-11 | 折叠选区（起止相同） | 表示光标位置，读回一致 |
| B-12 | 替换选区 | 选中部分被替换，其余不变 |
| B-13 | 选区越界（超过文本长度） | 夹到合法范围，不崩 |

### 3.3 只读与长度限制

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-14 | 只读状态下模拟输入字符 | 文本不变 |
| B-15 | 只读状态下调用替换选区 | **待定**：程序化修改要不要绕过只读？见第 7 节待确认项 |
| B-16 | 设最大长度后超量设文本 | 截断到上限 |
| B-17 | 最大长度设为 0 | 不限制 |

### 3.4 字符格式

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-18 | 选区设粗体后读回该区间格式 | 粗体位已置 |
| B-19 | 斜体、下划线同上 | 各自的位已置 |
| B-20 | 选区设颜色后读回 | 颜色一致 |
| B-21 | 只对部分选区设格式 | 区间外的字符格式不变 |
| B-22 | 设默认字体后，新输入的字符 | 采用新字体 |
| B-23 | 设默认字色 | 新输入字符采用新字色 |

### 3.5 段落格式

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-24 | 设居中对齐后读回 | 对齐方式一致 |
| B-25 | 设左缩进后读回 | 缩进值一致（注意单位是 twip） |
| B-26 | 多段落时只改当前段 | 其它段落不变 |

### 3.6 撤销与重做

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-27 | 输入后撤销 | 回到输入前的内容 |
| B-28 | 撤销后重做 | 回到撤销前的内容 |
| B-29 | 无可撤销时调用撤销 | 内容不变，不崩 |
| B-30 | 内容变化通知里带的撤销 / 重做标志 | 与实际是否新增了撤销记录一致 |

### 3.7 查找

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-31 | 向前查找命中 | 返回真，命中区间正确 |
| B-32 | 向后查找命中 | 同上 |
| B-33 | 查不到 | 返回假，选区不动 |
| B-34 | 区分大小写 | 大小写不一致时查不到 |
| B-35 | 不区分大小写 | 大小写不一致仍能查到 |
| B-36 | 整词匹配 | 子串不算命中 |
| B-37 | 绕回 | 从末尾向前查，绕回到开头后命中 |
| B-38 | 查找并选中 | 命中后选区落在命中处 |

### 3.8 持久化

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-39 | 存 RTF 再读回 | 文本内容一致 |
| B-40 | 带格式的内容存 RTF 再读回 | 粗体 / 颜色等格式保留 |
| B-41 | 存纯文本再读回 | 文本一致、格式丢失（这是预期行为） |
| B-42 | 读入非法 RTF | 返回失败，原内容不被破坏 |

### 3.9 自动增高

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-43 | 空内容时的期望高度 | 等于下限 |
| B-44 | 内容增加一行 | 期望高度增加约一行的高度 |
| B-45 | 内容超过上限 | 期望高度停在上限 |
| B-46 | 关掉自动增高 | 期望高度不再随内容变化 |
| B-47 | 换行开关影响期望高度 | 同一段文字，开自动换行时更高 |

### 3.10 运行期改属性（相对现有控件的关键改进，必须钉住）

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-48 | 多行改单行 | 生效，且**内容不丢**（现有控件此处要销毁重建） |
| B-49 | 单行改多行 | 同上 |
| B-50 | 只读切换来回 | 生效，内容与选区不丢 |
| B-51 | 自动换行开关切换 | 生效，内容不丢 |
| B-52 | 只改成员变量、不通知引擎 | **反向用例**：验证不生效——用来钉住"两步缺一不可"这条规则 |

### 3.11 引擎尚未创建时的行为

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| B-53 | 未创建时设文本再读回 | 走缓存，正常往返 |
| B-54 | 未创建时的选区操作 | 安全空转，读回 0，不崩 |
| B-55 | 未创建时存 RTF | 返回失败，且出参被清空 |
| B-56 | 未创建时的期望尺寸 | 返回下限，不崩 |
| B-57 | 创建之后 | 缓存内容同步进引擎，读回一致 |

---

## 4. C 类：布局用例

用现有布局测试的三段式写法：构造控件树、跑一次布局、逐个断言矩形。

| 编号 | 测什么 | 预期 |
| --- | --- | --- |
| C-01 | 固定尺寸提示下的矩形 | 与提示一致 |
| C-02 | 权重分配下的矩形 | 按权重分 |
| C-03 | 内边距生效 | 文本区 = 控件矩形减内边距 |
| C-04 | 边框开关影响文本区 | 关边框时文本区多出边框那一像素 |
| C-05 | 滚动条摆成覆盖式 | 滚动条矩形**与文本区重叠**，且文本区宽度**不因滚动条而缩减** |
| C-06 | 滚动条不显示时 | 文本区宽度与显示时相同（覆盖式的核心保证） |
| C-07 | 自动增高影响父容器布局 | 父容器按控件的期望高度分配空间 |
| C-08 | 自动增高关闭时 | 父容器按固定高度分配 |
| C-09 | **隐藏后再显示** | 重新显示后矩形正确 —— 这是 CLAUDE.md 明确要求覆盖的路径，只测"建出来就可见"测不到 |
| C-10 | 隐藏期间发生过重排，再显示 | 同上，且滚动条子控件的矩形也正确 |

---

## 5. D 类：需要真窗口

尽量少，且每条都要能解释"为什么非得有窗口"。

| 编号 | 测什么 | 预期 | 为什么需要窗口 |
| --- | --- | --- | --- |
| D-01 | 光标创建与销毁 | 创建后系统光标存在，控件失焦后被销毁 | 系统光标接口要求窗口句柄 |
| D-02 | 光标位置换算 | 引擎给的位置加上控件偏移后等于实际位置 | 同上 |
| D-03 | 光标在无焦点时不显示 | 未获得界面激活状态时请求显示光标，不显示 | 同上 |
| D-04 | 焦点进出 | 获得焦点时界面激活、失去时反激活 | 焦点是窗口级概念 |
| D-05 | 不接受焦点的开关 | 关掉后点击控件不获得焦点、不出现光标 | 同上 |
| D-06 | 滚动防递归 | 引擎要求设滚动位置时不形成无限循环 | 需要真实的引擎回调 |

---

## 6. E 类：只能人工核对

自动化不了，做成一张清单，每次改动相关代码后照着走一遍。

**输入法（第 6 步完成后必做，不通过不往下走）**
1. 微软拼音：输入中文，候选条位置是否跟随光标
2. 微软拼音：组字过程中界面是否正常刷新，有无残影
3. 搜狗输入法：同上两条
4. 系统日文输入法：同上两条
5. 输入法切换：中英文来回切，状态是否正确
6. 组字过程中点击别处：组字是否正确结束
7. 密码模式下：输入法是否被正确禁用

**视觉**
8. 光标闪烁节奏是否与系统其它输入框一致
9. 选区高亮配色是否正常，失焦后是否按预期隐藏
10. 滚动条覆盖在文字上是否难看（这条直接决定覆盖式方案是否成立）
11. 滚动条的淡入淡出时序
12. 边框在禁用 / 聚焦 / 悬停三态下的颜色
13. 占位文字的位置与颜色
14. 半透明 / 圆角背景下的渲染效果

**交互**
15. 拖选文字，鼠标移出控件范围后是否仍能继续选
16. 双击选词
17. 拖放：从资源管理器拖文件进来、从别处拖文字进来
18. 右键菜单的弹出位置（靠近屏幕边缘时不能跑到屏幕外）
19. 滚轮滚动的手感与步长

---

## 7. 需要你确认的三处预期行为

这三条我拿不准，写代码前要定下来，否则测试的预期值就是错的。

**一、只读状态下，程序化修改要不要被拒绝？**
用户输入被拒绝是肯定的。但业务代码调"设文本""替换选区"这类方法时，只读要不要拦？
我倾向**不拦**——只读约束的是用户，不是程序。公告预览面板就是只读的，但业务要往里填内容。
若采纳，B-15 的预期改为"程序化修改照常生效"。

**二、文本长度的计数口径。**
换行在引擎内部可能算一个字符也可能算两个，不同读取方式口径不同。
我倾向统一成**与设入字符串一致**的口径，即设入 N 个字符读回长度就是 N。
若采纳，B-05 的预期按此写，并在实现里做归一化。

**三、自动增高的下限默认值。**
我倾向默认下限为**一行的高度**，上限默认不限。这样单独放一个控件不会缩成零高，
而聊天输入框那种"最多五行"的需求由调用方显式设上限。

---

## 8. 覆盖率自查

| 功能点 | 覆盖用例 |
| --- | --- |
| 文本读写 | B-01 ~ B-08、B-53、B-57 |
| 选区 | B-09 ~ B-13 |
| 只读 | B-14 ~ B-15、B-50、D-05 |
| 字符格式 | B-18 ~ B-23、B-40 |
| 段落格式 | B-24 ~ B-26 |
| 撤销重做 | B-27 ~ B-30 |
| 查找 | B-31 ~ B-38 |
| 持久化 | B-39 ~ B-42 |
| 自动增高 | A-06 ~ A-11、B-43 ~ B-47、C-07 ~ C-08 |
| 拖放 | A-20、E-17 |
| 滚动条 | A-12 ~ A-16、C-05 ~ C-06、D-06、E-10 ~ E-11 |
| 运行期改属性 | B-48 ~ B-52 |
| 光标 | D-01 ~ D-03、E-08 |
| 输入法 | E-01 ~ E-07 |
| 右键菜单 | A-21 ~ A-24、E-18 |
| 布局 | C-01 ~ C-10 |
| 单位与坐标换算 | A-01 ~ A-05、D-02 |
| 属性位拼装 | A-17 ~ A-20 |

**合计**：A 类 24 条、B 类 57 条、C 类 10 条、D 类 6 条，自动用例 97 条；
E 类人工核对 19 项。

**已知的覆盖空白**（有意留白，说明理由）：
- 竖排文字：只有属性位拼装被覆盖，实际排版效果没有自动用例，靠人工看。
  理由是竖排没有业务调用方，投入产出比低。
- 密码字符：同上。
- 多显示器不同缩放下的表现：现有测试基础设施不支持模拟，靠人工。
